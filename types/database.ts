/**
 * PLACEHOLDER. Regenerar con:
 *   pnpm db:types   (supabase gen types typescript --local > types/database.ts)
 * una vez aplicada la migración 0001_init.sql. Hasta entonces, `any` mantiene
 * el proyecto compilando sin bloquear el andamiaje.
 */
export type Database = any
