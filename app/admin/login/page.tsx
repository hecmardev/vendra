import { AdminLoginView } from 'views/admin/login'

export const metadata = { title: 'Plataforma — Vendra' }

/** Login del superadmin de plataforma. */
export default function Page () {
  return <AdminLoginView />
}
